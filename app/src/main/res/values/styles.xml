<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="MyTheme"
        parent="@style/Theme.AppCompat.Light.DarkActionBar">
        <item name="android:actionBarStyle">@style/MyActionBar</item>

        <!-- Support library compatibility -->
        <item name="actionBarStyle">@style/MyActionBar</item>
    </style>

    <style name="MyActionBar"
        parent="@style/Widget.AppCompat.Light.ActionBar.Solid.Inverse">
        <item name="android:background">@mipmap/odsbanner</item>

        <!-- Support library compatibility -->
        <item name="background">@mipmap/odsbanner</item>
    </style>

    <style name="PreviewShortname" parent="TextAppearance.AppCompat.Title">
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">20dp</item>
        <item name="android:textAllCaps">true</item>
    </style>

    <style name="PreviewName" parent="PreviewShortname">
        <item name="android:textSize">16dp</item>
    </style>

    <style name="PreviewHighscore" parent="TextAppearance.AppCompat.Title">
        <item name="android:textSize">12dp</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="Response">
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">16dp</item>
    </style>

    <style name="Question" parent="TextAppearance.AppCompat.Headline">
        <item name="android:textSize">18dp</item>
    </style>

    <style name="ResumeListQuestion" parent="TextAppearance.AppCompat.Title">
        <item name="android:textColor">@color/cardview_dark_background</item>
        <item name="android:textSize">12dp</item>
    </style>

    <style name="ResumeListAnswer">
        <item name="android:textSize">14dp</item>
    </style>

</resources>
